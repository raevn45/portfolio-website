import './globals.css';

export const metadata = {
  title: 'Preshita Shinde — 2026',
  description: 'Builder, researcher, designer, organizer, professional rabbit-hole explorer.',
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
